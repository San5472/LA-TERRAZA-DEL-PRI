<p style="color: green"><%= notice %></p> 

  <%= render @carro %>  

    <div>
          <button type="button" class="btn btn-outline-info">
            <%= link_to "Edit this carro", edit_carro_path(@carro) %> |
            <%= link_to "Back to carros", carros_path %>
          </button>
          <br>
          <%= button_to "Destroy this carro", @carro, method: :delete %>
    </div>


  