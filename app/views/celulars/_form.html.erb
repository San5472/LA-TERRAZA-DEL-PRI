<%= form_with(model: celular) do |form| %>
  <% if celular.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(celular.errors.count, "error") %> prohibited this celular from being saved:</h2>

      <ul>
        <% celular.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :marca, style: "display: block" %>
    <%= form.text_field :marca %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
